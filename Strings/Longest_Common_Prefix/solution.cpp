class Solution {
    public:
        string longestCommonPrefix(vector<string>& strs) {
           string lcp = "";
           int n = strs.size();
           for(int i=0;i<strs[0].n;i++){
            char c=strs[0][i];
            for(int j=1;j<strs.size(); j++){
                
            }
           }
        }
    };